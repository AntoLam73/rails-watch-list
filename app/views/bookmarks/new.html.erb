<div class="m-3">
  <%# <%= link_to "Back to my lists", list_path(@list), class: "btn btn-secondary btn-sm mt-3" %> %>
</div>

<div class="container mt-4 mb-4 bg-light p-4 border rounded">

  <div class="mb-3 text-center">
    <h1>Add a new Movie</h1>
  </div>

  <div class="row">
    <%= simple_form_for [@list, @bookmark] do |f| %>
      <%= f.association :movie %>
      <%= f.submit "Create Bookmark", class: "btn btn-primary" %>
    <% end %>
  </div>

</div>
